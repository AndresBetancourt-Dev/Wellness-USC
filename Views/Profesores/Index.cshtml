@model IEnumerable<Wellness_USC.Models.Profesores>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Apell)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImageNameP)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Apell)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ImageNameP)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Profesores">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Profesores">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Profesores">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
